# 改动说明

## v2.3.0：MCP 技能连接器（外部工具接入）

对标 Cherry Studio 的 MCP Server 支持，把外部工具接进 AI 的工具链。

### 功能

- **支持两种传输**：`stdio`（本地命令，如 `npx -y xxx`）和 `HTTP`（远程端点）。
- **工具发现**：连接成功后自动列出服务器暴露的全部工具。
- **接入 AI**：MCP 工具以 `mcp_<serverId>__<toolName>` 命名，动态并入 function calling 的工具列表，单 AI / 多 AI / 研发模式都能用；新增或重连的服务器下一轮请求即生效。
- **设置界面**：「设置 → MCP 服务器」里添加 / 删除 / 重新连接，实时显示连接状态、工具数与错误信息。

### 安全

- MCP 工具随「AI 能力」总开关一起生效（默认关）。
- 服务器是用户自己加的，视为可信，调用不再逐个弹窗；但每次调用都记入工具日志。
- 连接失败只标记该服务器，不影响应用与其它服务器。

### 实现

- 新依赖 `@modelcontextprotocol/sdk`（v1.30.0）。
- 主进程 `src/main/mcp.js`：连接管理、工具发现、调用路由、状态查询。
- IPC 新增 `mcp:list` / `mcp:save` / `mcp:reload`；preload 暴露 `api.mcp.*`。
- `tools.js` 执行器加 MCP 路由分支；`agent.js` 支持 `getExtraToolDefs()` 动态注入工具定义。
- 渲染层 `features/mcp.js` + 设置分区，服务器配置存 `prefs.mcpServers`。

### 测试

新增 MCP 单测 12 项（工具名编解码、特殊字符净化、定义转换、参数透传、文本提取），`npm test` 全量 **268 项 · 0 失败**。

---

## v2.2.0：高级感打磨（视觉与微交互）

这一版不改功能，专注把「质感」提上去，整体更精致、专业、克制。

### 视觉

- **主按钮渐变**：发送 / 新建对话 / 主要确认按钮改为 indigo 渐变 + 柔和投影，悬停上浮、投影加深；「停止」态改用红色渐变。
- **排版**：全站字体抗锯齿更锐利，欢迎屏标题更紧凑。
- **头像/形象**：统一更柔和的投影。
- **输入框聚焦**：更柔的光晕（ring），不再生硬。
- **代码块**：圆角更精致 + 内层高光。
- **滚动条**：更细、更柔和，悬停加深。
- **欢迎屏**：顶部一层极淡的径向光晕，更有层次。

### 微交互

- **消息入场**：新消息轻轻滑入 + 淡入（0.3s，缓动曲线）。
- **卡片上浮**：快捷卡 / 技能卡 / 提示词卡悬停上浮并加投影。
- **按压反馈**：主要按钮点下去有轻微下沉（scale）。
- **状态点呼吸**：就绪态的绿点带轻微呼吸动画。
- **轻提示**：加毛玻璃背景 + 更深的投影。

以上全部遵循 `prefers-reduced-motion`（系统「减弱动态效果」开启时自动关掉动画），深色模式同步适配。

### 测试

`npm test` 全量 **256 项 · 0 失败**（纯视觉/动效改动，不动结构与逻辑，接线检查全绿）。

---

## v2.1.0：对标开源 AI 客户端的补强（提示词库 · 置顶 · 导出）

参照 Cherry Studio / Chatbox / ChatALL 等开源 AI 桌面客户端，补齐三项高频能力。

### 1. 自定义提示词库

对标 Chatbox 的「Prompt Library」、ChatALL 的「Prompt management」：

- 工具栏新增「提示词」按钮 + 右侧滑出面板，可**新建 / 删除 / 一键填入**常用提示词。
- 提示词存进 `prefs.prompts`（本地 config.json），纯本地，不新增 IPC 通道。
- 「填入输入框」把内容塞进输入框并聚焦，`Ctrl/⌘ + Enter` 快速保存。
- 命令面板新增「打开提示词库」。

实现：`features/prompt-lib.js`（新增，挂 `App.promptLib`）+ 面板 HTML/CSS + `prompts` 偏好。

### 2. 会话置顶

对标 Cherry Studio 的「Topic Management」：

- 会话列表每项新增 **📌 置顶**（悬停出现，置顶后常显）。
- 置顶会话排到列表最前；置顶状态随会话一起落盘，重启仍在。

实现：`data/sessions.js` 的 `togglePin` + 列表按 pinned 排序（只改显示顺序，不动底层数据，不影响现有索引逻辑）。

### 3. 导出会话

- 命令面板新增「导出当前会话为 Markdown」：把当前会话（含用户消息、各 AI 回答，剥掉 `[[SAVE:]]` 标记）导出为 `.md` 文件。

实现：`data/sessions.js` 的 `buildSessionMarkdown` + `exportSession`，复用已有的 `file:save` 通道。

### 测试

`npm test` 全量 **256 项 · 0 失败**（语法体检 45 个文件）。新增的提示词库、置顶、导出均通过渲染层冒烟与接线检查。

---

## v2.0.0：用户体验升级（主题 · 首屏 · 对话 · 性能 · 快捷键自定义）

这一版不动 AI 能力，专注把「日常用起来」的体验拉到一个新台阶。

### 1. 深色 / 浅色主题（含跟随系统）

- 新增「外观」设置区：**跟随系统 / 浅色 / 深色** 三选一，默认跟随系统。
- 深色模式覆盖了侧边栏、消息气泡、代码块、表格、所有浮层面板、设置视图、宠物气泡等全部主要界面。
- 主进程按主题偏好设置窗口底色，深色下启动**不再闪白屏**。
- 主题只打一个 `<html data-theme="dark">` 标记，样式全部由 `style.css` 末尾的覆盖块承担，逻辑与样式分离。

实现：`features/theme.js`（新增）+ `style.css` 深色覆盖块 + `config.js` 的 `theme` 偏好。

### 2. 启动与首屏

- 欢迎屏新增**「继续上次对话」**卡片：启动停在待机页时，一键回到最近一次聊到一半的会话。
- 会话列表每项右侧显示**相对更新时间**（刚刚 / N 分钟前 / 昨天 / M月D日）。
- 窗口底色跟随主题，见上。

### 3. 对话交互细节

- 消息时间戳从「固定 HH:mm」改成**相对时间**（刚刚 / N 分钟前 / 今天 HH:mm / 昨天），悬停可看完整日期时间。
- 修掉一个真问题：**「复制」AI 回答时不再带上 `[[SAVE:]]` 内部标记**，复制到的是干净正文。

### 4. 性能

- AI 头像（32 张 PNG）统一加 `loading="lazy"` + `decoding="async"`，长会话首屏不再一次性解码全部立绘。
- Markdown 渲染 LRU 缓存、流式渲染 60ms 节流、会话搜索 120ms 防抖（v1.13 已就位）继续保持。

### 5. 快捷键自定义

- 7 个组合键（命令面板 / 新建对话 / 折叠侧栏 / 设置 / 搜会话 / 导入文件 / 切换单多 AI）**全部可自定义**。
- 快捷键速查面板里的可自定义行点击后进入「录音」状态，按下新组合即保存，立即生效。
- 冲突检测：新组合若已占用会提示；可一键**恢复默认**。
- 存进 `prefs.shortcuts`，跟随配置落盘；`Esc` / `?` / `Enter` 这类基础键保持不变，不参与自定义。

实现：`features/shortcuts.js` 重写（注册表 + 匹配 + 录音面板），`app.js` 的全局按键改走 `App.shortcuts.match()`。

### 测试

`npm test` 全量 **256 项 · 0 失败**。新增的快捷键自定义、主题、首屏恢复均在渲染层冒烟与接线检查覆盖范围内（可自定义行沿用 `.sc-row`，速查面板条目数校验不变）。

---

## v1.14.0：表格、多 AI 团队配置、去掉界面上的表情包

### 1. 表格：从「一堆竖线」变成真表格

以前 AI 写表格，界面和导出两侧都不完整：

| 位置 | 改之前 |
|---|---|
| 对话气泡 | 只认严格首尾竖线；列数少一个就整行错位；没有对齐；宽表把版式撑破 |
| 导出 docx | `mdToDocxParagraphs` **根本不认识表格**，表格行被当成普通段落，导出的 Word 里是一竖排 `\|` 文本 |
| 导出 PDF | `mdToHtml` 同样没有表格分支 |
| 导出 xlsx | `parseTable` 无脑按逗号切列 —— 「你好，帮我看下这个」会变成两列 |

现在统一走一套解析（`src/main/files.js` 的 `collectTable` / `splitTableRow`，界面和导出共用同一套规则）：

- 少写结尾竖线也认（`姓名 | 年龄` 这种）
- 对齐语法生效：`---` 左 / `:---:` 居中 / `---:` 右
- **列数不齐自动补齐**，宁可留空格也不让整行错位
- 气泡里套 `.table-scroll`，宽表横向滚动，不撑破版式
- docx 用 `Table / TableRow / TableCell` 真表格渲染（表头浅底加粗）
- PDF 出 `<table>`，配套样式已加
- xlsx：只认真表格；整篇都不像表格、且每行逗号数一致时才按 CSV 切
- 每张表右上角有「复制表格」，复制成 **TSV**，直接能粘进 Excel / 表格文档

### 2. 多 AI：团队按钮 + 可折叠的配置面板

**工具条上直接看得到团队**：多 AI 模式下，原来的 AI 选择器换成一颗「团队」按钮，
显示「统领 DeepSeek · 1 位专家」，点击即开配置，鼠标悬停能看完整名单。
没定统领或没选专家时按钮变琥珀色提示，不用点进去才发现不能发。

**配置面板重做**：

- 两个分区（统领 / 专家）**点标题即可折叠展开**，用 `max-height` 过渡 + 箭头旋转，
  是「滑上去」不是硬切 `display:none`；状态同步写进 `aria-expanded`
- 列表区可滚动，15 家全展开也不会把面板顶出屏幕
- 每家一行：形象 + 名称 + 密钥状态（已保存 · 结尾 xxxx / 未配置）
- **没配密钥的置灰不可选**，点了会告诉你先去设置里填
- 专家区有「全选已配置的 / 清空」，标题右侧实时显示选中数
- 面板正文整体可滚动，不再随内容无限长高

### 3. 去掉界面上的装饰性 emoji（共 50 处）

工具条按钮、消息操作按钮、面板标题、工具块 / 计划卡 / 时间线标题、命令面板图标、
技能卡图标，全部换成文字或单字标记：

| 位置 | 改前 | 改后 |
|---|---|---|
| 模式切换 | 🤖 单AI / 🎯 多AI | 单 AI / 多 AI |
| 工具条 | 📂 导入 · 🛠 研发模式 · 🎯 技能 · 💭 深度思考 · 🪙 · ⚙️ 设置 | 导入 · 研发模式 · 技能 · 深度思考 · 用量 · 设置 |
| 消息操作 | 💾 另存为 · 📋 复制 · 🔄 重新生成 · 🗑️ 删除 | 另存为 · 复制 · 重新生成 · 删除 |
| 命令面板 | ⚙️ 🎯 📂 🤖 💭 🛠 📁 🧹 ☰ 📋 🗑️ 💬 | 设 技 文 单 多 团 思 研 目 清 栏 复 会 聊 |
| 技能卡 | 💼 💻 ✍️ 🌏 📊 🎓 📈 👨‍🏫 🎨 🎤 ⚖️ 🩺 | 办 编 写 译 数 论 商 教 创 面 法 康 |
| 其它 | 🧰 AI 用过的工具 · 🛠 执行过程 · 🗺 执行计划 · ✅ 已自动保存 · ⚠️ 出错 | 纯文字标题 |

**保留**的：文件类型图标（📘📗📙📕 这类是识别信息）、厂商形象与头像（那是形象不是装饰）。
密码框的 👁 换成了「显示」文字。

### 测试

新增 37 项，**全量 256 通过 · 0 失败**：

- 表格解析 10 项：表头/行数/缺列补齐/结束位置、无首尾竖线也认、分隔行判定、
  xlsx 表格切分、普通句子不被逗号切碎、整齐 CSV 仍按逗号切、PDF HTML 里的 table 只出现一次
- 界面表格 7 项：渲染成真 table、滚动容器、对齐生效、列数补齐、分隔行不漏进正文、
  气泡里真渲染出来、复制按钮挂载
- 多 AI 团队 12 项：团队按钮显示统领与专家数、面板列表渲染、计数、清空/全选、
  折叠与 aria-expanded、保存后面板关闭且团队按钮同步

---

## v1.13.0：拆文件 + 修三个真 bug（`npm test` 219 项全过）

重构本身不产生新功能，所以这一节只写三件事：怎么拆的、**顺手抓出来的三个真 bug**、以及现在怎么自测。

### 1. 拆文件：2000 行 main.js / 4000 行 renderer.js → 39 个模块

原来的 `main.js` 和 `renderer.js` 两个巨型文件，改什么都得先滚半天屏幕。现在按职责拆开，
**没有用打包器和 ES module**（`file://` 下 ES module 会被 CORS 拦住），所以渲染层沿用
`window.App` 命名空间 + `index.html` 里按依赖顺序排的 `<script>`，主进程侧正常用 `require`。

主进程 `src/main/`（9 个）：

| 文件 | 负责 |
|---|---|
| `index.js` | 入口：单实例锁、拼装各模块、窗口生命周期 |
| `window.js` | 窗口创建（`contextIsolation: true` / `nodeIntegration: false`） |
| `providers.js` | 15 家厂商的地址、模型、thinking 参数、余额接口 |
| `config.js` | 配置读写（原子写盘 + `safeStorage` 加密密钥） |
| `ai.js` | 请求构造、SSE 解析、用量归一化、中止 |
| `tools.js` | 8 个工具 + 安全底线（硬拦截路径 / 命令黑名单） |
| `agent.js` | 工具调用主循环（研发模式 40 轮 / 普通 8 轮） |
| `files.js` | 文件导入与目录摘要、docx/xlsx/pptx/pdf/md 生成 |
| `ipc.js` | 所有 IPC 通道 |

渲染层 `src/renderer/`（30 个）分五层：`core/`（dom、state、utils、markdown、tokens、bridge、
save-markers、steps）、`data/`（constants、skills、prompts、sessions）、`ui/`（avatars、blocks、
messages、gen-status）、`features/`（chat、ai、attachments、provider-ui、agent、panels、
tools-ui、settings、pet、command、shortcuts）、顶层 `app.js` + `boot.js`。

两条硬规矩：
- **`getElementById` 只在 `core/dom.js` 出现一次**，其它模块一律用 `App.dom.xxx`
- **模块之间不互相绑事件**，绑定集中在 `app.js` 的 `bindEvents()`，单个模块能独立读懂、独立换掉

顺带的性能改动：
- Markdown 渲染结果加了 LRU 缓存（同一段正文重复渲染不再重解析一遍）
- 流式渲染从「每帧全量重排」改成 60ms 节流 + 尾帧必渲染（`throttleTrailing`），长回答不再 O(n²)
- 会话搜索输入防抖 120ms，不再每敲一个字重建整个列表
- 系统提示词拼装结果缓存，不再每次发消息重拼

### 2. 抓出来的三个真 bug

#### ① 渲染层根本起不来：`mainEl` 没定义

`renderer.js` 里用了 9 次 `mainEl`，但**全文件没有声明过它**，HTML 里也只有 `<main class="main">`
（没有 id）。这是脚本顶层的 `ReferenceError` —— 不是「某个功能坏了」，是**整个界面打不开**。
现在 `core/dom.js` 里 `mainEl: bySel('.main')` 统一取。

#### ② 重启后搜索密钥会丢

`config.js` 重载配置时把 `toolKeys` 整个丢了（联网搜索填的 key 每次重启都要重填），
而且 `setToolsPrefs` 只保其中一个开关、把另外两个冲掉。现在重载保留工具密钥，
三个开关一起留。

#### ③ 启动跑两遍 → 所有开关「点了没反应」

`boot.js` 调一次 `start()`，测试/上层再调一次，`bootstrap()` 就跑了两遍，
**所有事件监听被绑两遍**：点一下深度思考，开完立刻又关，看起来像开关坏了。
研发模式、自动保存、桌面宠物全都这个症状。

修法是让 `start()` 幂等 —— 第二次调用直接返回第一次的 promise：

```js
let startPromise = null;
function start() {
  if (startPromise) return startPromise;
  ...
  startPromise = bootstrap().catch(...);
  return startPromise;
}
```

另外修了「计划卡做完就消失」：流式那个气泡结束时会被换掉，挂在它上面的执行计划跟着没了。
现在计划会写进消息（`aiMsg.plan`）并搬到最终气泡上，重开会话也能看到。

### 3. 自测：`npm test`

以前测试是临时脚本、跑完就删。现在固化成 `tests/`，`npm test` 一把跑完 **219 项，全绿**：

| 段 | 覆盖 |
|---|---|
| 语法体检 | 43 个文件逐个 `node --check` |
| 主进程单测 | 15 家厂商配置、配置落盘/重载、SSE 解析、错误码、用量归一化、工具安全底线、降级指令解析、agent 主循环、文件导入与文档生成 |
| 渲染层冒烟 | **用 jsdom 把 index.html + 全部脚本真跑一遍**，假 `window.api` 走完发消息、流式、工具卡、停止生成、出错、多 AI、附件、会话增删、命令面板、技能、设置、宠物、快捷键、落盘 |
| 接线检查 | 代码引用的 DOM id 是否都存在、调用的 `api` 方法是否都暴露、preload 的通道是否都有 handler、跨模块 `App.X.Y` 名字是否对得上、渲染层不出 `require(` 和硬编码密钥 |

`tests/run.js --plain` 会出一份纯文本报告（`tests/.report.txt`），CI 里也能读。

新增一条硬约束：**事件回调里抛的异常在 jsdom 里不会打断测试**，所以冒烟里专门接了
`error` / `unhandledrejection`，结尾断言「全程零未捕获异常」。

### 备份

改之前的 `main.js` / `renderer.js` 原件留在 `.backup/`，确认新版没问题后可以整个删掉。

---

## v1.12.0：新厂商全部有了 AI 画的形象 + 密钥管理折叠

### 1. 7 家新厂商的形象（AI 绘图，参照原有画风）

上一版加的 7 家（混元 / 星火 / 千帆 / Gemini / Groq / OpenRouter / xAI）一直用 emoji 兜底。
这次用图生图把形象补齐了，**每家都以现有头像当风格模板**（正头参照智谱的半身挥手构图，
Q 版参照 DeepSeek 的二头身），所以 15 家摆在一起画风是统一的，不是两批东西：

| 厂商 | 形象设计 |
|---|---|
| 腾讯混元 | 蓝紫渐变长发 + 头顶「混元漩涡」光环 |
| 讯飞星火 | 天蓝双马尾 + 金色星形发饰 + 火花粒子 |
| 百度千帆 | 藏青波浪长发 + 小白帆发饰 + 海军风水手服 |
| Gemini | 银紫长发 + 双子星发饰 + 蓝紫渐变裙 |
| Groq | 橙色短发 + 黑底橙电路纹夹克 + 速度线 |
| OpenRouter | 紫青渐变长发 + 三线汇聚（路由）发饰 |
| xAI Grok | 黑白单色 + 白色 X 形发夹，表情偏酷 |

每家两张（正头 + `-chibi`），1024 生成后缩到 512×512 PNG，体积 160~310KB，与原有素材一致。
生成后用 Pillow 压平为白底（和前 8 家同规格）。

`AVATAR_ART` 白名单同步加入 7 家 —— 头像、Q 版切换、桌面宠物、AI 选择器全部自动生效，不用改其它代码。

### 2. 密钥管理折叠（手风琴）

15 家密钥表单全部摊开的话一屏根本看不完。现在**一行一家**：

- 折叠行只显示：头像 + 名称 + 状态（已保存·结尾 xxxx / 未配置 / 密钥已失效）
- 点开才见模型选择和密钥输入框；**展开一家自动收起上一家**，来回比较不乱
- 已配置的排前面（保留原有排序）；一家都没配过时默认展开第一家，让你知道从哪填
- 分区标题上挂「2/15」小徽标，不用展开就知道配了几家
- 折叠头是真 `<button>`，键盘可操作，focus-visible 有焦点环

### 3. 设置分区折叠

四个分区（密钥管理 / AI 能力 / 对话参数 / 数据）全部支持点标题收起展开：
密钥管理默认收着（下面有 15 行），其余默认展开。箭头带转向动画。

### 测试

22 项全过：手风琴的展开/收起/互斥/再点收起、默认展开策略（配过全收起 / 全没配展开第一家）、
计数徽标 2/4、AVATAR_ART 15 家 × 正头+Q版 = 30 张 PNG 磁盘存在性校验、
HTML 里 4 个 `data-sec` 与分区 id 一一对应、`key-count` 徽标存在、分区折叠事件已绑定。
三个 JS 语法通过，CSS 括号配平（518/518）。

---

## v1.11.0：拖拽终于能用了（含文件夹）+ 研发模式

### 1. 拖拽是个假功能 —— 根因是 Electron 44

「把文件拖到对话框上，什么都不显示」不是 UI 没刷新，是**路径根本没取到**。

代码里写的是：

```js
const files = Array.from(e.dataTransfer.files);
for (const file of files) {
  if (file.path) await attachFileByPath(file.path);   // ← file.path 恒为 undefined
}
```

`File.path` 从 Electron 32 起被废弃、到现在（本项目是 `electron@^44.4.1`）已经拿不到了。
`if (file.path)` 恒为假，整个 drop 分支等于空转 —— 拖十个文件也是什么都不会发生。

正解是 `webUtils.getPathForFile(file)`，而它只能在 preload 里调（渲染层没有 Node）。
所以：

- preload 新增 `api.files.pathFor(file)`，内部 `file.path || webUtils.getPathForFile(file)`，
  老版本也能兜住，取不到就返回空串而不是抛异常。
- 拖放监听从 `.main` 挪到 `document`（侧边栏上松手也能接住），
  并补一层 window 级 `preventDefault`，防止 Electron 直接导航去打开被拖的文件。
- 拖完给汇总提示「已加入 N 项，M 项读取失败」，不再是无声无息。

### 2. 文件夹也能拖了

以前拖目录进来，`readFileContent` 会返回一句「这是一个目录，不是文件」，白拖。

新增 `file:ingest` 通道：文件和目录走同一个入口，主进程自己判断类型。目录会展开成
**目录树 + 关键文件正文** 两样东西：

- 目录树带文件大小，最多 400 行
- 文本文件按「先浅后小」挑，默认 40 个 / 12 万字以内；读不下的会明确告诉模型
  「还有 N 个文件没展开，需要哪个单独 read_file」
- 自动跳过 `node_modules` / `.git` / `dist` / `.venv` / `__pycache__` 等依赖和构建目录，
  并在开头说明跳过了多少个 —— 不然 AI 会以为项目里没有文件
- 异步遍历 + 双预算（6000 节点 / 10 秒），在 `C:\Users` 这种大目录上也不会卡死界面

附件条上目录是独立的样式：📁 + 目录名 + 「N 个文件」；文件显示真实体积（以前显示的是
内容字数换算出来的假 k 值，现在改成 B/KB/MB）。移除按钮改用元素引用删除，
不再用 forEach 下标（列表变动时会删错行）。

### 3. 研发模式：让 AI 自己把任务做完

工具栏新增 **🛠 研发模式**，打开后多出一条工作台：工作目录 + 自动批准开关。

**工作目录** —— 用原生选目录对话框选一个文件夹，AI 就知道该在哪干活。
没选之前，自动批准按钮不显示。

**它自己会做的循环**（写进系统提示，渲染层和主进程各补一份）：

1. 勘察：`list_dir` / `read_file` / `search_files` 先摸清现状，不许一上来就问一堆问题
2. 计划：用新的 `set_plan` 工具把步骤推给界面（3-8 条），用户实时能看到
3. 执行：一次一件事，`write_file` 必须写**完整可运行的文件**，禁止「……」「其余省略」
4. 验证：能跑就 `run_command` 跑，报错就读报错、改、再跑
5. 汇报：改了哪些文件、验证结果、哪些假设需要确认、还剩什么

配套改动：

- **工具轮数从 8 提到 40**（`AGENT_MAX_ROUNDS`）—— 8 轮干不完一个项目，以前写到一半就断了
- **跑满轮次不再回空白**：会再问一次模型，让它把已完成的成果总结出来
- **执行过程时间线**：研发模式下工具调用排成带序号的「第 N 步 + 状态」，
  比原来一排 chip 好读，且同一工具连调三次能看出是三次
- **计划卡**：`set_plan` 推过来的步骤渲染成清单，插在回答气泡上方；重复调用只更新不叠加
- **命令面板**加了「开启/关闭研发模式」「选择研发工作目录」
- 多 AI 模式也接了同一套开关

### 4. 自动批准的风险边界

独立干活时每次写文件都弹窗，等于不独立。所以加了「自动批准」，但**只在勾选了工作目录、
且目标路径确实在工作目录内**时才生效：

```js
function insideWorkDir(workDir, target) {
  const norm = (p) => String(p).replace(/\//g, '\\').replace(/\\+$/, '').toLowerCase();
  return t === root || t.startsWith(`${root}\\`);   // C:\work 不会误伤 C:\work2
}
```

目录外一律照旧弹窗，弹窗里还会说明「你开了自动批准，但这次不在目录内，所以仍然问你」。
自动批准过的内容照常写进操作日志，设置里能回看。

**硬底线没有松动**：`isBlockedPath`（系统目录 / `.ssh` / `.aws` / `.gnupg` 等）和
命令黑名单的判定都排在 `askPermission` 之前，自动批准绕不过去。测试里专门验了这个顺序。
自动批准默认关，而且没选工作目录时强行置为关（防止上次的状态残留成空白支票）。

### 测试

主进程 38 项 + 渲染层 44 项 + 接线检查 8 项，全部通过：

- 造了一个真实临时目录（含 `node_modules`、嵌套子目录、二进制文件）跑 `buildDirectoryBrief`
  和 `file:ingest`，逐条验目录树内容、跳过规则、字节统计
- `insideWorkDir` 的四个边界：目录内 / 目录外 / 同名前缀 `C:\work` vs `C:\work2` / 空路径
- `askPermission` 三种组合：目录内放行、目录外仍弹窗、没开则弹窗
- 拦截顺序：硬底线和命令黑名单都在自动批准之前
- 渲染层：目录 chip 样式与文件数、真实体积显示、去重、移除、拖拽去重与失败提示、
  时间线合并规则（running→done 合成一行、不同调用各占一行）、计划卡更新不叠加、
  研发提示词只在研发模式下注入
- 93 个元素 ID 与 HTML 对齐，三个 JS 文件语法检查通过

---

## v1.10.0：供应商扩到 15 家 + 可调对话参数 + 会话标题终于像话了

### 1. 新增 7 家供应商（8 → 15）

| 新增 | 接口地址 | 默认模型 |
|---|---|---|
| 腾讯混元 | `api.hunyuan.cloud.tencent.com/v1` | hunyuan-turbos-latest |
| 讯飞星火 | `spark-api-open.xf-yun.com/v1` | lite |
| 百度千帆 | `qianfan.baidubce.com/v2` | ernie-speed-128k |
| Google Gemini | `generativelanguage.googleapis.com/v1beta/openai` | gemini-2.0-flash |
| Groq | `api.groq.com/openai/v1` | llama-3.3-70b-versatile |
| OpenRouter | `openrouter.ai/api/v1` | openai/gpt-4o-mini |
| xAI Grok | `api.x.ai/v1` | grok-3 |

全部走 OpenAI 兼容协议，不用改主流程。混元和星火的地址是查官方文档确认过的；
**千帆、xAI 的模型名以各自控制台为准**，所以都开了「可直接手填模型名」。

顺带处理了两个扩员带来的问题：
- 新厂商没有立绘 PNG，以前会显示成碎图。现在没素材的自动退回 emoji，
  桌面宠物和侧边栏退回吉祥物形象。
- 供应商变多后设置页会很长，**已配置密钥的自动排到最前面**，中间加一条分隔线。

### 2. 对话参数可以细调了

设置里新增「⚙️ 对话参数」区：

- **温度** —— 滑杆 0~1.5，实时显示数值。日常 0.7、写代码 0.2、想点子 1.0
- **最大输出 token** —— 填 0 表示不限制
- **携带历史轮数** —— 从原来的「对话」区搬过来
- **单次请求正文上限** —— 默认 60000 字符，超了自动丢更早的内容
- **自定义系统提示词** —— 长期生效的额外要求，追加在内置提示词之后，和技能提示同时生效
- **用 AI 给会话起标题** —— 开关
- **自动保存生成的文件** —— 关掉后 AI 不再产出文件标记，内容直接写在回复里
- **恢复默认参数** —— 一键回到出厂值（不动密钥和技能）

以前 `ai:stream` 根本没接 temperature 和 maxTokens，这两个参数在主进程里是死代码 —— 现在接通了，单 AI 和多 AI 都生效。

关掉自动保存后，万一模型还是产出了文件标记，会明确提示「内容没有落盘」，不会让你以为存下来了。

### 3. 会话标题终于像话了

以前是**直接截断你输入的前 14 个字**：「帮我整理桌面」的标题就是「帮我整理桌面」。

现在加了本地规则提取，不依赖 AI、立刻生效：
- 剥掉客套话：「帮我 / 麻烦你 / 请 / 能不能 …」，叠几层都能剥干净
- 去掉量词：「写一份周报」→「写周报」
- 只取第一个分句，后面的补充说明不算标题
- 上限 12 字，超了加省略号

实测效果：

| 你的输入 | 以前的标题 | 现在的标题 |
|---|---|---|
| 帮我整理桌面 | 帮我整理桌面 | 整理桌面 |
| 帮我整理一下桌面吧 | 帮我整理一下桌面 | 整理桌面 |
| 帮我写一份本周项目周报，包含完成事项、进行中、下周计划 | 帮我写一份本周项目周报，包 | 写本周项目周报 |
| 请帮我总结一下这篇文章的中心思想 | 请帮我总结一下这篇文章的中 | 总结这篇文章的中心思想 |

AI 标题生成仍然会跑，在规则提取的基础上再润色一次；生成失败或你关掉了自动命名，
就用规则提取的结果，不会退回成截断。

另外**双击会话标题可以自己重命名** —— AI 起的名不合心意随时改。

### 测试

主进程 18 项 + 渲染层 21 项，全部通过：
15 家供应商逐家走一次真实请求构造（确认 `thinkingParam` / 地址 / 模型解析都没问题）、
新参数默认值、temperature 与 maxTokens 真的进了请求体、
7 组标题提取用例、参数控件确实写回 prefs、87 个 DOM id 全部存在。
三个 JS 文件语法检查通过，CSS 括号配平。

---

## v1.9.0：交互品质打磨（键盘优先 · 状态可见 · 内容质量）

方向是「把 Codex 那套交互品质搬过来，保留自己的定位」：形象、桌面宠物、技能卡槽全留着，
改的是手感和完成度。

### 键盘优先

- **命令面板**（`Ctrl` / `⌘` + `K`）：一个输入框里同时搜命令、切 AI、跳会话。
  上下键选择、回车执行；带分组和分类标签。
- **快捷键**：`⌘K` 命令面板、`⌘N` 新建对话、`⌘B` 折叠侧边栏、`⌘,` 设置、
  `⌘F` 搜会话、`⌘⇧O` 导入文件、`⌘⇧M` 切单/多 AI、`?` 看速查表、`Esc` 关浮层。
  欢迎屏上直接把 `⌘K` 露出来，不用猜。
- 按 `?` 弹出速查面板，不用再翻文档。
- 焦点在输入框里时 `?` 不会被当快捷键吃掉。

### 状态可见

- **生成状态条**：输入区上方实时显示「正在思考 / 正在使用工具 / 正在深度思考 / 正在回答 / 正在汇总」
  和已经跑了多久。多 AI 下还会显示「N 位专家并行处理中」。
- **按停止不再白等**：以前一点停止，已经生成的内容直接消失。现在会保留中断时的内容，
  打上「⏹ 已手动停止」标记，存进会话；多 AI 被中断时，规划和已跑完的专家结果也会留下。
  停止和出错在界面上是两回事，不再混为一谈。
- **会话搜索**：侧边栏顶部可搜标题和消息正文，命中处高亮。

### 内容质量

- **系统提示词重写**：原来只有一句「你是一个专业办公助手」。现在是明确的回答准则
  —— 结论先行、用 Markdown、不确定就说不确定、不编造，以及更清楚的文件生成约定。
- **12 个技能提示词全部重写**：原来每个技能只有一句「你现在是一位 XX」，等于没说。
  现在每个都有角色定位 + 具体工作方法（比如周报按四段组织、代码审查分三档排优先级、
  法律顾问必须标出缺失条款）。
- **多 AI 编排改进**：规划、专家、汇总三段提示词都重写，加了明确的格式约束和输出要求。
  多 AI 现在也会跟随深度思考开关（之前那个开关对它完全无效）。
- **标题生成**：附件正文不再参与，否则标题会变成文件名。
- **SAVE 解析加固**：模型经常不规矩 —— 标记里多空格、正文套代码围栏、忘了写闭合标记。
  现在这些都能兜住，文件名还会做安全化处理（防 `../../` 路径穿越）。

### 消息操作

- 用户消息可以**就地编辑并重发**（改一句不用删掉整段重打），`Enter` 提交、`Esc` 取消。
- AI 回答可以**单独删除**。

### 趣味部分加强

- 宠物有了状态感知：思考中 / 刚存完文件 / 刚出错 / 深夜 / 余额告急，说的话不一样。
- 气泡里加了「当前单 AI 还是多 AI + 挂了几个技能」。
- 生成中宠物浮动变快，像在忙。
- 空闲台词和提示都补了新内容（会告诉你 `⌘K` 和 `?`）。

### 无障碍

- 全站统一的键盘焦点环（鼠标点不显示，键盘走查清晰可见）。
- 消息操作按钮在键盘聚焦时也会显示，不用非得把鼠标移上去。

### 测试

打桩加载渲染层跑 27 项，全部通过：含真实触发一次发送消息、触发「已停止」分支、
SAVE 解析的 7 种边界、会话搜索、以及「代码引用的 79 个 DOM id 在 HTML 中都存在」。
三个 JS 文件语法检查通过，CSS 括号配平。

顺带修掉一个真 bug：`sendMessageInternal` 里 `userMsg` 先使用后声明，
会触发 TDZ 导致发送消息直接崩 —— 是这次加真实流程测试才抓到的。

---

## v1.8.1：修掉「能力总开关」这个假开关，以及几处会卡死/丢数据的问题

这一版不动功能，只修 bug。其中第一条是安全相关的，建议优先看。

### 1. 设置里的「允许 AI 使用这些能力」总开关，其实是假的

v1.8.0 里写着「总开关默认关闭，必须你在设置里显式打开」，但代码里有两处绕过了它：

- `ai:once`（多 AI 通道）里 `useTools` 写死成 `true`，完全不看开关
- `executeTool` 里 `web_search` / `web_fetch` 在 `toolsEnabled()` 判定**之前**就 return 了

结果是：开关显示"关"，多 AI 下的总指挥和每个专家照样能联网搜索。
界面上那份「安全底线」承诺，和真实行为对不上。

现在：总开关在函数入口统一拦一次，联网搜索和本地访问一起管；`ai:once` 跟随开关。
已用打桩测试双向验证 —— 关着不带 `tools`、开着才带。

### 2. 主进程同步 IO 会把整个界面冻住

`list_dir` / `search_files` 用的是同步 `readdirSync`，跑在主进程里。
在 `C:\Users` 这种大目录下搜一次，界面会完全卡住几十秒，期间连窗口都拖不动，
而且**没法中断**（看门狗只管网络请求）。

现在改成异步，并给「时间 8 秒 + 节点 2 万」双预算，到点提前收工并在工具日志里标注。

### 3. 读文件没有体积上限

`readFileContent` 原来直接把整个文件读进内存，读完才截取。
一个几 GB 的日志或视频被当文本读进来，主进程会当场崩掉（等于应用闪退）。

现在先 `stat` 看大小，超过 20MB 直接拒读并说明原因。

### 4. 点「停止」之后还会偷偷重试一次

`callOnceWithRetry` 不区分中止和真出错。多 AI 模式下按停止，
其中一次请求抛异常 → 800ms 后自动重跑一次 → 用户以为停了，其实还在烧 token。

现在中止单独走 `aborted` 通道：不重试、不弹红色错误框、多 AI 的每个角色都真停。
顺带修了「同一个 reqId 下多个并行请求只能停掉最后一个」的问题。

### 5. 关窗口时最后一段会话可能丢不掉盘

`beforeunload` 里调的是异步 `invoke`，进程已经走了，写盘来不及。
新增同步通道 `sessions:save-sync`，只在退出时用一次。

### 6. 其它

- 自动存文件失败会把已经拿到的回答一起带走 —— 现在先落地消息，存失败单独提示
- 多 AI 模式一直没传 `thinking`，深度思考开关对它无效 —— 已补上
- 附件正文整份写进 `sessions.json`，多轮下来文件会滚到几十 MB —— 存档时按 12000 字截断（发给模型的仍是全文）
- 切到空会话时 token 数字不归零
- 工具循环跑满 8 轮时可能返回完全空白的回复 —— 兜底退回最后一次输出

### 测试

打桩加载主进程跑 10 项：总开关双向生效、大文件拒读/正常文件照读、
中止安全返回、同步保存落盘、`tools:get` 状态正确 —— 全部通过。
三个 JS 文件语法检查通过。

---

## v1.8.0：AI 能力扩展（联网搜索 + 本地访问）+ 修好「深度思考」假开关

### 先说一个查出来的假功能：深度思考只有 2 家是真的

之前界面上那个 💭 深度思考开关，看起来 8 家都能开，实际一查代码：

| 厂商 | 真实参数 | 改之前 |
|---|---|---|
| DeepSeek | `thinking:{type:'enabled'}` + `reasoning_effort` | ❌ 空实现 |
| 智谱 GLM | `thinking:{type:'enabled'}` | ✅ 已接 |
| 通义 Qwen | `enable_thinking: true` | ✅ 已接 |
| 豆包 | `thinking:{type:'enabled'}` | ❌ 空实现 |
| OpenAI | `reasoning_effort: 'high'` | ❌ 空实现 |
| Kimi | 仅 k2-thinking 自带 | ❌ 空实现 |
| 硅基流动 | 取决于模型 | ❌ 空实现 |
| Ollama | 取决于本地模型 | ❌ 空实现 |

**8 家里 6 家的开关开了毫无反应，还静默失败** —— 你以为在深度思考，其实什么都没发生。

现在的处理：
- 真正支持的 5 家（DeepSeek / 智谱 / 通义 / 豆包 / OpenAI）按各家官方写法接上了
- Kimi、硅基流动、Ollama 明说**不支持**，界面上开关置灰划掉，鼠标悬停提示「开关对它无效」
  —— 不再假装能用
- DeepSeek 思考模式下不接受 `temperature`，会自动去掉，避免报错

### 联网搜索

- 默认**免密钥**（DuckDuckGo），开箱即用
- 设置里可填 Tavily / Serper / Bing 的 key，填了自动切到更高质量的结果
- 另有 `web_fetch` 直接打开网页读正文（已剥离 HTML 标签）
- 12 秒超时，失败不阻塞对话

### 本地访问

8 个工具：列目录 / 读文件 / 搜文件 / 写文件 / 删除 / 执行命令 / 联网搜索 / 打开网页。

安全底线（**这些不随开关放开**）：
1. 总开关默认关闭，必须你在设置里显式打开
2. 系统目录、盘根目录、`.ssh` / `.aws` / `.gnupg` 一律硬拒绝
3. 命令有黑名单：`format` / `rm -rf /` / `shutdown` / `reg delete` / fork 炸弹等直接拒
4. **写文件、删除、执行命令每次都弹窗确认**，路径和命令都给你看，默认焦点在「拒绝」
5. 所有动作记日志，设置里可回看最近 40 条

### 「每个 AI 都能用」—— 这里有个坑，已修

单 AI 走流式通道，多 AI 走的是另一条非流式通道（`ai:once`），
**原本多 AI 下的总指挥和所有专家都用不了任何工具**。现在两条通道都走同一套
agent 循环，多 AI 的每个角色都能搜索和读文件，工具过程实时汇总显示在同一条消息里。

不支持工具调用的模型会自动降级成「文本指令」模式（`SEARCH: 关键词` 这种），
保证 8 家都能用，不会出现「开了开关但没反应」。

### 界面上的变化

- 设置里新增「AI 能力」区：总开关 / 允许执行命令 / 搜索服务商选择 / 操作日志
- 消息里会显示「🧰 AI 用过的工具」，每条带图标、摘要和状态（进行中 / 完成 / 已拒绝 / 失败）
- 深度思考不支持当前 AI 时，按钮置灰划掉并给出提示

### 测试

主进程 24 项、界面 8 项，全部通过。覆盖了：默认关闭不带工具、工具循环、
结果回传、用量累加、系统目录拒绝、命令黑名单、删除需确认、降级重试、
5 家思考参数、2 家正确标为不支持、多 AI 通道工具全链路。

---

## v1.7.0：待机界面 + 对话框集成 + 修折叠 bug

### 修掉了「设置开着时点☰，比例很怪」

根因是 CSS 优先级打架：`.sidebar.settings-mode` 把侧边栏加宽到 344px，
而 `.sidebar.collapsed` 想把它收成 0 —— 两条规则权重相同，写在后面的赢了，
结果是**侧边栏已经看不见了，却还占着 344px**，右边内容被挤成怪比例。

两层修复：

- 折叠时如果正开着设置，**先退出设置视图再折叠**（点☰ 的语义变成「收起侧边栏」）
- CSS 加 `.sidebar.collapsed.settings-mode` 兜底，保证折叠永远赢过加宽

实测：设置态 344px → 点☰ 后 0px 且回到会话列表 → 再展开 260px 恢复正常。

### 启动就是待机界面

每次打开都停在「你好，我能帮什么忙吗？」的待机页（一个空白新对话），
历史会话照样在左侧列表里点得开。载入时会把空会话滤掉，所以**不会越积越多**。

### 小助手每回启动都回右下角

启动时统一回到右下角默认位，并清掉之前存的位置。
（想改成「记住上次拖到哪」，`renderer.js` 启动那段有注释说明，一行替换即可。）

### 所有操作集成到输入框，顶栏只剩标题

顶栏现在只有 ☰ 和「AI 助手」。原本散在顶栏的按钮全部并进输入框上方两排工具条：

| 第一排 | 🤖 单AI / 🎯 多AI · 带形象的 AI 选择器 · 多AI配置 |
|---|---|
| 第二排 | 📂 导入 · 🎯 技能 · 💭 深度思考 · 🪙 token · （弹性空隙）· ⚙️ 设置 |

打字的手不用离开输入框，设置被弹性空隙推到最右边，不容易误触。

---

## v1.6.1：按反馈调整浮窗

### 修掉了拖动时图标不在鼠标下面

拖的时候图标会跑到鼠标**左边一大截**，偏移量正好等于侧边栏宽度。根因是算抓取偏移时把
「鼠标的视口坐标」和「元素在 main 内的相对坐标」混着减，多减了一次 `mainRect.left`。
现在统一用视口坐标算抓取点、落位时再统一换算，**实测抓中心拖到任意点，偏差 0px**。

### 图标稍微放大

88px → **104px**，气泡、投影、动画同步跟着放大。

### 单 AI / 多 AI 搬到下面的输入框

原来「🤖 单AI / 🎯 多AI」和 AI 选择器都在顶栏右边，手要往上够。现在整条搬到了
**输入框上方**的工具条里，跟打字的地方在一起：

- 模式开关（单AI / 多AI）
- 带形象的 AI 选择器（下拉改成**向上弹**，原来向下会被输入框挡住）
- 多 AI 配置按钮（只在多 AI 模式出现）

顶栏只留下导入、技能、深度思考、token 和设置。

### 气泡改成只显示「它自己」的信息

之前一打开就是 8 家的余额清单，太吵。现在点一下，出来的就是**当前这一个 AI 的账本**：

| | |
|---|---|
| 头部 | 它自己的形象 + 名字 + 一句会变的话 |
| 我的余额 | 单独一大块，右侧可刷新 |
| 本会话 tokens | 这个会话累计用了多少 |
| 当前模型 | 现在用的是哪个模型 |
| 底部 | 密钥状态（已保存 · 结尾 xxxx / 失效 / 未配置）+ 进设置 |

**换 AI 时余额会自动重查**，不会沿用上一家的数字。而且接口查询也改成只请求当前这一家，
不再一次性骚扰所有平台。

---

## v1.6.0：右下角可拖动的桌面宠物浮窗浮窗

右下角有个只会卖萌摆动的角色，现在它变成一个**真正的信息浮窗**，而且能拖到屏幕任何地方。

### 能拖动

- 按住角色拖到哪儿都行，位置**写进设置，重开还在原位**
- 自动贴边：怎么拖都不会被拖出窗口
- **双击**一键回右下角原位
- 拖动时手感有反馈（放大 + 轻微倾斜），拖动结束后紧跟的那次 click 会被吃掉，不会误开面板

### 点头像弹出气泡

气泡里是实时信息，不是装饰：

| 区块 | 内容 |
|---|---|
| 头部 | 当前 AI 的形象 + 名字 + 一句会变的话 |
| 概览 | 已接入 4 / 8 家、本会话累计 tokens |
| 余额 | 逐家列出账户余额，支持刷新 |
| 脚注 | 哪些平台支持查余额 + 一键进设置 |

气泡会根据位置自动翻转（靠左就往右弹、靠上就往下弹），不会出屏。

### 那些「有趣的话」是看情况说的

不是随机复读同一句，而是根据状态挑：

- 正在生成 → 「正在替你想，稍等一小会儿…」
- 一家都没配 → 「带我去设置里配一个吧」
- **余额低于 2 元 → 「再不充值我就要饿肚子了…」**
- 深夜 → 「这么晚还在忙？早点睡，我先替你看着余额」
- 其余时候在闲聊池里随机，偶尔混一句使用提示

### 余额查询：只接有官方接口的三家

查了各家文档，只有这三家的余额接口是公开且稳定的：

| 平台 | 接口 |
|---|---|
| DeepSeek | `GET https://api.deepseek.com/user/balance` |
| Kimi（月之暗面） | `GET https://api.moonshot.cn/v1/users/me/balance` |
| 硅基流动 | `GET https://api.siliconflow.cn/v1/user/info` |

**智谱没有公开的余额接口**（网上流传的那个是内部抓包地址，随时会失效），火山方舟和阿里要控制台会话鉴权 —— 这三家一律在界面上标注「无公开余额接口」，**不去猜、不发无效请求**。

实现细节：

- 请求在主进程发出，`Bearer` 头用的是已经加密存好的密钥，渲染层拿不到任何凭据（CSP 仍是 `connect-src 'none'`）
- 单次 8 秒超时，某一家挂了不影响其他家
- 结果缓存 60 秒，打开气泡时才重新查，不会在后台偷偷轮询
- 金额小于 10 元显示 4 位小数，避免看到 `¥4.32` 其实是 `¥4.3207`

---

## v1.5.0：去掉小助手、设置进侧边栏、密钥管理

### 1. 去掉右下角小助手

右下角那个常驻的卡通角色按钮和气泡面板整块移除了（HTML / CSS / JS 全部清空）。
原来挂在它身上的那些快捷操作改由侧边栏设置承担。`assets/avatars/mascot*.png` 两张素材先留着没删，
哪天想把它放到欢迎页之类的地方，换回来只改一處的地方即可。

### 2. 顶部 AI 选择器换成带形象的自定义下拉

原来的原生 `<select>` 只有 emoji 和文字。现在每一项都显示该 AI 自己的漫画形象 + 名称 + 状态：

- 选中哪家，按钮上和左下角侧边栏里立刻换成那家的形象
- 状态行直接写「已保存 · 结尾 xxxx」，不用猜有没有配
- 没配密钥的项点一下会直接跳到密钥管理，而不是默默不可选
- 原生 select 降级为 `sr-only` 隐藏控件，只用来承载取值逻辑，改动面最小

### 3. 设置搬进侧边栏

浮动在那个右侧的设置面板改成了侧边栏里的第二个视图：

- 会话列表 ↔ 设置，两个视图互斥切换，左上角「←」返回
- 进入设置时侧边栏自动加宽到 344px（宽度带过渡动画）
- 入口：左下角 AI 信息区、侧边栏底部「⚙️ 设置与密钥」、欢迎页引导按钮
- 密钥管理放在设置最上面第一位，不再是藏在下面的普通一节

### 4. 点击主屏幕自动收起

所有浮层（AI 下拉、技能面板、多 AI 配置面板）统一收口到 `closeFloatingLayers()`：

- 在主区域任意位置点一下就收起，不用再去找关闭按钮
- Esc 键行为保持
- 侧边栏设置是「视图」不是浮层，不会因为点了主区域就被关掉

### 5. 密钥：专门的管理区 + 明确的保存反馈

之前其实已经加密落盘了（Windows DPAPI / macOS Keychain），但界面上输入框每次都空着，
看着像没存，于是反复重填。现在：

- 每一家都明确标出「已保存 · 结尾 xxxx」（只显示末 4 位，不泄露完整密钥）
- 密钥解不开时会标红提示「密钥已失效，请重新填写」，不会无声失败
- 输入框支持回车直接保存、点👁临时看明文、单独清除某一家
- 保存成功的提示语改成「下次打开自动带上」

> 落盘链路已验证：写入 → 模拟重启 -> 重新读取，两家密钥都在，磁盘文件里查不到明文。

---

## v1.4.0：双形态形象（正常版 / Q 萌版）

每个角色现在有**两套形态**，素材共 18 张：

| 文件名 | 形态 | 说明 |
|---|---|---|
| `assets/avatars/{id}.png` | 正常版 | 腰部以上半身立绘，双臂可见，默认展示 |
| `assets/avatars/{id}-chibi.png` | Q 萌版 | 二头身 chibi，大头小身体、包子脸、超大眼睛，俏皮表情 |

覆盖全部 9 个角色：DeepSeek 鲸鱼女仆 / 智谱 猫头鹰 / 通义 云朵 / Kimi 月兔 / 豆包 豆芽 / 硅基 水晶 / OpenAI 旋风 / Ollama 羊驼 / 右下角小助手。

### 形态按状态灵动切换

用户选择的是「按状态切换」而不是手动开关，所以做成了自动的：

- **默认** —— 正常版
- **开始生成（思考中）** —— 页面上的 AI 形象统一切成 Q 萌版，配原本的歪头思考动画
- **回答完成** —— 保持萌版并触发「开心」蹦跳，停留 2.6 秒后自动切回正常版
- **自动保存文件成功** —— 短暂亮出萌版 1.8 秒
- **请求出错** —— 立刻恢复常态，不做庆祝动作

切换瞬间会有一个弹跳缩放入场动画（`avatar-pop`），并且跟随系统的「减弱动态效果」设置自动关闭。

### 素材处理

- 全部抠成**透明背景 PNG**（从四角做连通洪水填充，角色内部的白色高光不会被误删，边缘羽化 3px 消白边）
- 自动裁掉底部孤立水印带，按内容裁紧后居中放入 512×512 画布
- 右下角小助手改为**展示完整全身**（`object-fit: contain` + 投影），不再是圆形裁切的小头像，角色会“站”在界面上

想换形象直接替换对应 png 即可；想调整切换时长改 `renderer.js` 里的 `CHIBI_HOLD_MS`。

---

# v1.3.0：AI 绘制形象 + 右下角小助手

**形象全部换成 AI 绘制的拟人立绘**（以一张水蓝色鲸鱼女仆图为风格基准，图生图保证 8 个角色画风一致），不再是表情包：

| 角色 | 形象 |
|---|---|
| 🐳 DeepSeek | 水蓝色鲸鱼女仆（鲸鱼=最大的哺乳动物） |
| 🧠 智谱 GLM | 猫头鹰少女（圆框眼镜、羽毛披肩、抱着古书） |
| ☁️ 阿里 通义 | 云朵少女（暖橙云朵盘发、捧着小云） |
| 🌙 Kimi | 月兔少女（银白长发、抱着弯月） |
| 🫘 豆包 | 豆芽少女（头顶嫩芽、元气笑） |
| 🔬 硅基流动 | 水晶少女（护目镜、托着发光晶体） |
| 🌀 OpenAI | 旋风少女（翠绿螺旋长发、环绕绿叶） |
| 🦙 Ollama | 羊驼少女（毛茸茸羊驼耳、捧着热饮） |
| ✨ 小助手 | 右下角常驻角色（青紫渐变发、头顶光球、挂脖耳机） |

素材在 `assets/avatars/`，**想换形象直接替换对应 png 即可**，代码不用动。

### 右下角小助手

- 常驻右下角，待机时轻轻上下浮动；**生成回复时会歪头晃动**（思考状态），**保存文件成功时会连蹦三下**（开心状态）
- 点击弹出**气泡快捷面板**，不用进设置就能：切换当前 AI、开关深度思考、管理技能卡槽、调携带轮数，底部一键进完整设置
- 没配密钥时，小助手会主动冒出来打个招呼引导你去设置
- 点面板外面自动收起

### 左下角侧边栏

底部新增**当前 AI 信息区**：立绘头像 + 名字 + 就绪状态（未配置会标黄），点击直达设置。展开侧边栏一眼就能看到现在用的是谁。

---

# v1.2.0：更多厂商 + 虚拟形象 + Token 统计

**新增 5 家厂商**（共 8 家，见下表），每家都有专属形象与配色：

| | 厂商 | 默认模型 | 备注 |
|---|---|---|---|
| 🐳 | DeepSeek | deepseek-flash | |
| 🧠 | 智谱 GLM | glm-4-flash | |
| ☁️ | 阿里 通义 | qwen-plus | |
| 🌙 | Kimi（月之暗面） | moonshot-v1-8k | |
| 🫘 | 豆包（火山方舟） | doubao-1-5-pro-32k-250115 | 模型名填方舟的**接入点 ID** |
| 🔬 | 硅基流动 | Qwen/Qwen2.5-7B-Instruct | |
| 🌀 | OpenAI | gpt-4o-mini | |
| 🦙 | Ollama（本机） | qwen2.5 | **无需密钥**，需先启动 Ollama 服务 |

- **模型可手填**：设置里模型字段改成 输入框 + 候选列表，既能选预设也能自己写（本地模型名很随意，必须能手填）。
- **虚拟形象出现在**：消息气泡顶部（头像 + 名称）、多 AI 协作的每个阶段卡片、设置面板的厂商卡片、顶部下拉框。
- **Token 统计**：
  - 优先用服务商返回的真实用量（流式请求会自动带 `stream_options` 让对方回传 usage）；
  - 对方不返回时按字符数估算（中日韩 1 字符≈1 token，其余 4 字符≈1 token），并标注「（估算）」；
  - 每条 AI 回复下方显示 `🪙 提示 1.2k · 补全 3.4k`；
  - 顶部常驻会话累计用量，点击看明细；
  - 多 AI 模式会把「1 次规划 + N 次专家 + 1 次汇总」的用量累加起来。

---

# v1.1.0：安全与体验重构

这一版把代码评审里提出的问题全部修掉了，顺带做了一轮体验升级。

## 先做这件事：轮换你的 API Key

旧版本把三家的密钥明文写在 `renderer.js` 顶部。**这些密钥应当视为已泄露，请到各家控制台吊销并重新生成**，然后在应用内「设置」里重新填写。

`.backup/` 里保留了改动前的原始文件，其中的密钥已自动抹除。确认新版没问题后可以直接删掉整个 `.backup` 目录。

## 安全

| 问题 | 处理 |
|---|---|
| 密钥明文硬编码 | 密钥改由主进程用 `safeStorage` 加密存盘（Windows DPAPI / macOS Keychain），渲染层完全拿不到 |
| `nodeIntegration: true` + 关闭上下文隔离 | 已改为 `nodeIntegration: false` + `contextIsolation: true`，新增 `preload.js` 用 `contextBridge` 暴露受限接口 |
| Markdown 链接注入 | 渲染先整体转义（含引号），链接再做协议白名单（只放行 http/https/mailto/tel），异常协议退化为纯文本 |
| `xlsx` 0.18.5 漏洞 | 升级到 0.20.3（SheetJS 官方源） |
| 生成 PDF 时 HTML 未转义 | `mdToHtml` 改为先转义再解析 |
| 页面可任意跳转 / 开新窗 | 拦截 `will-navigate` 与 `setWindowOpenHandler`，外链交给系统浏览器；`index.html` 加了 CSP |

## 功能缺陷

- **会话存档静默失败**：`_loadingDiv` 这个 DOM 节点挂在 session 上，导致 `JSON.stringify` 抛错被吞、每次发消息都存不上。已改为模块级变量。
- **会话存储**：从 localStorage 迁到 `userData/sessions.json`，不再受 5MB 限制，写入改为原子操作；技能、多 AI 配置、偏好也一并落盘。
- **附件**：单文件读取上限 6 万字并提示截断；请求只携带最近 N 轮，附件内容不会每轮重复发送。
- **深度思考**：之前只写了个开关、从未传给模型。现已接入请求参数（各家用各自的 thinking 字段）。
- **上下文管理**：新增「携带历史轮数」设置（默认 6 轮），并有 6 万字总预算兜底；输入框上方实时显示本次将发送多少条。
- **流式渲染**：改为 `requestAnimationFrame` 节流，每个动画帧最多重排一次。
- **`pdf-parse` v2 API 变更**：旧的 `pdfParse(buffer)` 在 v2 下会直接抛错（PDF 导入其实是坏的），已改用 `new PDFParse(...).getText()`，并识别无文字层的扫描件。
- **重新生成**：不再依赖「最后一条恰好是用户消息」的隐含假设，改为向前查找最近一条用户消息并截断。
- **空会话堆积**：启动时只在确实没有会话时才新建。
- 其他：`loadFile` 改绝对路径；错误信息按 HTTP 状态码给出人话；新增 90 秒无数据超时。

## 体验升级

- **设置面板**（⚙️ 按钮或侧边栏底部）：密钥填写 / 清除 / 一键跳转申请页、每家模型可切换、历史轮数、清空会话，并显示数据存放目录。
- **密钥状态可见**：未配置的 AI 在下拉框里置灰，标题栏状态点变黄，欢迎页出现「去配置」引导。
- **Toast 提示**替代所有 `alert` / `confirm`。
- **智能滚动**：你手动往上翻看历史时不再被强行拽回底部，右下角出现「回到最新」按钮。
- **思考过程实时可见**：深度思考内容边生成边展示，不用等结束。
- **附件卡片**显示体积（如 `12k`），可单独移除。
- **代码块**显示语言标签，复制失败有反馈。
- **上下文计量条**：实时显示「本次发送 X/Y 条 · 约 N 千字」。
- 侧边栏底部新增设置入口；`Esc` 可关闭任意面板；`Enter` 发送、`Shift+Enter` 换行；窗口尺寸调大，启动不再闪白屏；单实例锁定（重复启动会聚焦已有窗口）。

## 怎么用

```bash
npm start          # 启动
npm run dist       # 打包（Windows 会出 NSIS 安装包 + 免安装版）
```

首次启动请先点右上角 ⚙️ 填入至少一家密钥，之后即可正常对话。
